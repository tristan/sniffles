(ns sniffles.active-project)

; This is a convenience namespace. the sniffles/manage function will bind the settings map of the project that called it to this
; which allows other functions to easily get access to these settings without having to do anything crazy

(declare settings)
